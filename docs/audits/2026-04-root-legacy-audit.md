# 2026-04 루트 레거시 파일 감사

> **대상 독자**: 운영·감사 — 피처 014에서 수행한 루트 레거시 safe 제거 기록.

**이슈**: #313 (v2.6.0) · **피처 브랜치**: `014-landing-docs-refresh`

본 피처의 US4 (선택 트랙) 로 루트 디렉터리 레거시를 검증하고, "제거해도 안전함이 확인된" 파일만 처리했다.

## 대상별 판정

| 파일 | 분류 | 참조 검색 결과 | 외부 시스템 상태 | 결정 | 비고 |
|------|------|----------------|-----------------|------|------|
| `02_honeymoon_plan.md` | personal-legacy | `grep -rn "02_honeymoon"` → 0건 (spec·자기 자신 외) | 해당 없음 | **remove** | 피처 014 커밋에서 `git rm`. Git 히스토리엔 보존. |
| `_config.yml` | jekyll-residue | `specs/travel-search/001-...research.md`에 v1 시절 설정 예시 흔적만 | `gh api repos/.../pages` → `status: built`, `cname: trip.idean.me` (Pages 자체는 활성 상태) | **keep (defer)** | Pages 자체가 active. DNS는 Vercel로 향하나 Pages 빌드는 살아있음. 완전 정리는 Pages 설정 해제와 함께 후속 이슈로 분리. |
| `index.md` | jekyll-residue | `_config.yml`과 한 쌍 | 동일 | **keep (defer)** | 상동. |
| `claude_desktop_config.example.json` | mcp-artifact | `specs/travel-search/005-ax-api-mcp/tasks.md`·`specs/export/002-bundle-ical-mcp/tasks.md`에서 업데이트 대상으로 참조 | 설치 스크립트·문서 레퍼런스 | **keep** | 현재 활용 중. |
| `manifest.json` | mcp-artifact | `specs/travel-search/005-ax-api-mcp/plan.md`에서 엔트리포인트 지정 | MCP 서버 manifest | **keep** | 현재 활용 중. |

## 후속 제안

- **Jekyll 잔재 완전 정리**: GitHub Settings > Pages에서 Pages 배포 해제 → 워크플로/PR에서 Pages 관련 반응 확인 → 이상 없으면 `_config.yml`·`index.md` 제거. 별도 이슈로 분리(v2.6.1 또는 이후) 권장.
- 디렉터리 중복 의혹(`mcp` vs `mcp-servers`, `src` vs `web`)은 본 피처 스코프 밖(스펙 Clarifications #4). 후속 이슈 검토 대상.

## 검증

- `npm run lint` → 통과
- `npx tsc --noEmit` → 통과
- e2e·unit 회귀는 피처 014의 Polish 트랙(T021~T023)에서 수행.

---

*Generated by feature 014-landing-docs-refresh on 2026-04-20.*
