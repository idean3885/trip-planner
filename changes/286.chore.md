**CI에 lint/typecheck/test 게이트 추가**: `.github/workflows/ci.yml` 신설. PR과 develop push 단계에서 `npm run lint`(blocking) / `npx tsc --noEmit`(blocking) / `npm test`(blocking)를 자동 검증하고, `npm run test:coverage`는 `vitest.config.ts` threshold(lines 100 / branches 95)가 아직 baseline 미달(`92.8%`)이라 #282 복원 전까지 non-blocking(`continue-on-error`)으로 둔다. Node 20 matrix. 이전에 누적된 baseline lint error(`tailwind.config.ts` `require()`)는 v2.4.3 shadcn 도입으로 config 파일 자체가 사라지면서 자연 해소됨.
