**CI에 lint/typecheck/test 게이트 추가**: `.github/workflows/ci.yml` 신설. PR과 develop push 단계에서 `npm run lint` / `npx tsc --noEmit` / `npm test`를 자동 검증. `npm run test:coverage`는 baseline 미달 상태라 #282 복원 전까지 non-blocking. 동반으로 `tailwind.config.ts`의 `require()` → ESM `import`로 수정하여 기존 lint error(1건) 해소, lint 게이트 blocking 안정화.
