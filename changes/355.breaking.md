**Google Calendar 연동: 여행당 1개 공유 캘린더 모델로 재설계 (v2.9.0 expand)**: 기존 per-user `GCalLink`는 유지되지만, 새로 제공되는 `TripCalendarLink` + `MemberCalendarSubscription` 테이블로 여행당 캘린더가 1개만 존재하도록 바뀐다. v2.8.0에서 연동한 여행은 오너의 기존 캘린더를 그대로 공유 캘린더로 승격 재사용하며, 같은 여행의 다른 멤버가 만든 DEDICATED 캘린더는 앱 내 연결이 자동 해제된다(외부 계정의 캘린더 자체는 유지 — 사용자가 직접 정리 가능). 후속 릴리즈에서 레거시 API/테이블은 제거된다(contract 단계).
