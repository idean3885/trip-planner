**캘린더 provider 추상화 토대 도입 (foundation)**: `CalendarProvider` 인터페이스·`getProvider` registry·DB `provider` 컬럼(GOOGLE 기본값)을 추가. 라우트는 기존 동작 그대로 유지(stub만 도입, 호출 없음 → 사용자 가시 변경 0). 왜: 후속 Apple iCloud CalDAV 도입(#417) 전에 두 provider를 동일 모양으로 다룰 토대 필요. expand-and-contract 패턴의 expand 1단계.
