**디자인 시스템 기반(shadcn/ui) 도입 + 폼 6종 마이그레이션**: `src/components/ui/`에 초기 12종(button·input·label·field·card·dialog·dropdown-menu·select·separator·skeleton·sonner·tabs) vendoring 후, 기존 폼 컴포넌트 6종(ActivityForm·AuthButton·DeleteTripButton·LeaveTripButton·InviteButton·TodayButton)을 shadcn Button·Input·Label·Dialog 기반으로 마이그레이션. `window.confirm` → `Dialog`, `alert()` → `toast.error()`/`toast.success()`로 교체해 접근성·포커스 트랩·일관된 상태(hover/active/disabled) 확보. 서버 API fetch 엔드포인트·요청 인자·props 시그니처는 1:1 보존(헌법 V). 루트 레이아웃에 `<Toaster />`(라이트 고정) + 개발 전용 컴포넌트 카탈로그 `/components` 신설(`(dev)` route group + 프로덕션 `notFound()` 이중 차단). 라이트 단독 정책: shadcn이 자동 주입한 `.dark` 블록 제거, `@custom-variant dark`만 남겨 템플릿 컴파일 호환(런타임 inert). 신규 의존성: `@base-ui/react`, `class-variance-authority`, `tailwind-merge`, `clsx`, `lucide-react`, `tw-animate-css`, `sonner`.
